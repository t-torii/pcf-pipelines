---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: google/cloud-sdk
    tag: alpine

inputs:
- name: pcf-pipelines

run:
  path: pcf-pipelines/tasks/gcp/pks-api-setup/task.sh

params:
  GCP_PROJECT_NAME:
  GCP_REGION:
  GCP_ZONE:
  GCP_SERVICE_KEY:
  PKS_API_LB_NAME:
